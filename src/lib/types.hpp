#pragma once
#include "types.h"

// I have to keep this in a cpp file but I want x macros
#include <vector>
typedef std::vector<char> Bytes;
typedef std::vector<const char *> Names;
typedef std::string String;